name: Llibrary CI

on:
  push:
    branches:
      - main

jobs:
  library:
    uses: MrRamPoudel/shared-workflows/.github/workflows/dotnet-library.yml@v1

    permissions:
      contents: write
      packages: write